@semantq/storage/
├── index.js                    # Main exports
├── providers/
│   ├── index.js               # Provider registry
│   ├── BaseProvider.js        # Abstract base class
│   ├── LocalProvider.js       # Local filesystem
│   ├── UploadThingProvider.js # UploadThing cloud
│   ├── S3Provider.js          # AWS S3
│   └── CloudinaryProvider.js  # Cloudinary (optional)
├── services/
│   └── StorageService.js      # Core service with provider management
├── models/
│   └── FileStorage.js         # Database model (Prisma)
├── middleware/
│   └── upload.js              # Multer/upload middleware
├── utils/
│   ├── fileUtils.js           # File helpers (mime, hash, path)
│   └── config.js              # Configuration loader
├── migrations/
│   └── create_file_storage.sql
└── README.md